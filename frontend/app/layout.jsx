export const metadata = {
  title: "BQ19",
  description: "Sistema BQ19",
};

export default function RootLayout({ children }) {
  return (
    <html lang="pt-BR">
      <body>{children}</body>
    </html>
  );
}
